// См. комментарий в app/settings/account/page.tsx — та же логика.
// Положи рядом с ним, в ту же route group, что и остальные защищённые страницы.
export default function SettingsGeneralPage() {
  return null;
}
